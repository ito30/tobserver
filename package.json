{
  "name": "tobserver",
  "version": "1.0.0",
  "description": "Tiket Observer",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "node_modules/sequelize-cli/bin/sequelize db:migrate --config databases/mysql/config/config.js --migrations-path databases/mysql/migrations"
  },
  "author": "Ito",
  "license": "ISC",
  "devDependencies": {
    "sequelize-cli": "^2.7.0"
  },
  "dependencies": {
    "mysql": "^2.13.0",
    "node-schedule": "^1.2.3",
    "tlogger": "git+ssh://git@github.com/tiket-dev/tiket-microservice-lib-logger"
  }
}
